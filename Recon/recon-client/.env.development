# Checked in for shared local dev defaults (`npm start`).
# Override anything here in `.env.development.local` (gitignored).
REACT_APP_API_BASE_URL=http://localhost:4000
REACT_APP_REPORTS_BASE_URL=http://localhost:4001
